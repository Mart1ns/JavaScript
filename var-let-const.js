var altura = 5;
var comprimento = 7;

area = altura * comprimento;
console.log(area)

// boaaaa
// só podemos usar a variável elt, depois que inicializamos ela
// o const protege o fluxo da apicação, já n é possível altear o valor dela, não podemos só declarar, temos q inicializar tb
// == valor // === valor e tipo